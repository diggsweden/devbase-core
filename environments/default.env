# SPDX-FileCopyrightText: 2025 Digg - Agency for Digital Government
#
# SPDX-License-Identifier: CC0-1.0

#!/usr/bin/env bash
# Default Environment Configuration Template
# Copy this file to default.env and modify as needed
# For organization-specific config, use devbase-custom-config/config/org.env
#
# IMPORTANT: Do NOT set these core path variables here (they are readonly):
#   DEVBASE_ROOT, DEVBASE_LIBS, DEVBASE_DOT, DEVBASE_FILES, DEVBASE_ENVS, DEVBASE_DOCS
# These are set by setup.sh and cannot be overridden.

# Locale Configuration (OPTIONAL)
# Set this to install a specific locale on the host OS
# Example: DEVBASE_LOCALE="sv_SE.UTF-8" for Swedish
# DEVBASE_LOCALE=""

# Email Domain Configuration (OPTIONAL)
# Set this to pre-fill email domain for Git configuration
# Example: DEVBASE_EMAIL_DOMAIN="@company.com"
# Default: "@" (no prefill)
DEVBASE_EMAIL_DOMAIN="@"

# SSH Key Configuration (OPTIONAL)
# Supported types: ed25519 (recommended), ecdsa (P-521 curve), ed25519-sk, ecdsa-sk
DEVBASE_SSH_KEY_TYPE="ed25519"

# Key filename (will be created in ~/.ssh/)
# Recommended formats:
#   - Ed25519: id_ed25519_${identifier}
#   - ECDSA: id_ecdsa_521_${identifier}
DEVBASE_SSH_KEY_NAME="id_ed25519_devbase"

# Proxy Configuration (OPTIONAL)
# Set host and port if behind corporate proxy (empty = disabled)
# DEVBASE_PROXY_HOST="proxy.company.com"
# DEVBASE_PROXY_PORT="8080"
# DEVBASE_NO_PROXY_DOMAINS="*.company.com,*.internal,.com.se,github.com"
# Supports: wildcards (*.domain.com), suffix (.domain.com), exact (github.com)

# Container Registry (OPTIONAL)
# Set host and port if using internal registry (empty = disabled)
# Set in devbase-custom-config/config/org.env for organization-specific settings
# DEVBASE_REGISTRY_HOST="registry.company.com"
# DEVBASE_REGISTRY_PORT="5000"  # Container registry port (common: 443, 5000, 8082)

# Package Registry Configuration (OPTIONAL)
# These settings control where development tools download packages and dependencies.
# Set specific registry URLs with full paths for each tool.
#
# PyPI Registry (Python packages)
# Examples:
#   Nexus: https://nexus.company.com/repository/pypi-proxy/simple
#   Artifactory: https://artifactory.company.com/artifactory/api/pypi/pypi-virtual/simple
#   Generic: https://pypi.company.com/simple
# DEVBASE_PYPI_REGISTRY="https://nexus.company.com/repository/pypi-proxy/simple"
#
# NPM Registry (Node.js packages)
# Examples:
#   Nexus: https://nexus.company.com/repository/npm-proxy/
#   Artifactory: https://artifactory.company.com/artifactory/api/npm/npm-virtual/
#   Generic: https://npm.company.com/
# DEVBASE_NPM_REGISTRY="https://nexus.company.com/repository/npm-proxy/"
#
# Cypress Download Mirror (Cypress binary)
# Examples:
#   Nexus: https://nexus.company.com/repository/cypress-mirror/
#   Generic: https://cdn.company.com/cypress/
# DEVBASE_CYPRESS_REGISTRY="https://nexus.company.com/repository/cypress-mirror/"
#
# Testcontainers Image Prefix (container images)
# Examples:
#   Harbor: registry.company.com:5000/
#   Nexus: nexus.company.com:8082/
#   Generic: registry.company.com/
# DEVBASE_TESTCONTAINERS_PREFIX="registry.company.com:5000/"

# Organization Domain (OPTIONAL)
# Used for automatic NO_PROXY configuration
# DEVBASE_DOMAIN="company.com"

# User Preferences (OPTIONAL)
# Set these to skip interactive prompts
# DEVBASE_GIT_AUTHOR="Your Name"
# DEVBASE_GIT_EMAIL="your.email@company.com"
# DEVBASE_SSH_KEY_ACTION="keep"  # or "new" to generate new key
# DEVBASE_THEME="everforest-dark"  # or "everforest-light"

# Locale Configuration (OPTIONAL)
# Default is en_US.UTF-8 if not set
# Common values: en_US.UTF-8, sv_SE.UTF-8 (Swedish), en_GB.UTF-8 (British)
export LANG="en_US.UTF-8"
# LC_ALL="en_US.UTF-8"

# REQUIRED - User ID (usually auto-detected)
export USER_UID=$(id -u)