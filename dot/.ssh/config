# SSH Configuration Template
# DevBase default SSH configuration with strong security
#
# Order matters - first match wins!
# 1. Personal config (highest priority - can override everything)
# 2. Organization config (can override devbase defaults)
# 3. DevBase defaults (lowest priority)
#
# To add SSH hosts:
#   - Personal: ~/.config/ssh/user.config
#   - Custom/Organization: ~/.config/ssh/custom.config

# Personal SSH hosts (highest priority - can override everything)
Include ~/.config/ssh/user.config

# Custom/Organization SSH hosts (can override devbase defaults)
Include ~/.config/ssh/custom.config

# DevBase defaults (lowest priority - catch-all)
Host *
  # Security settings
  HashKnownHosts yes
  AddKeysToAgent yes
  StrictHostKeyChecking yes
  VisualHostKey yes
  
  # Modern cryptography (ED25519 preferred, with secure fallbacks)
  HostKeyAlgorithms ssh-ed25519,ssh-ed25519-cert-v01@openssh.com,ecdsa-sha2-nistp521,rsa-sha2-512,rsa-sha2-256
  PubkeyAcceptedAlgorithms ssh-ed25519,ssh-ed25519-cert-v01@openssh.com,ecdsa-sha2-nistp521,rsa-sha2-512,rsa-sha2-256
  KexAlgorithms curve25519-sha256,curve25519-sha256@libssh.org,ecdh-sha2-nistp521,diffie-hellman-group16-sha512,diffie-hellman-group18-sha512
  Ciphers chacha20-poly1305@openssh.com,aes256-gcm@openssh.com,aes128-gcm@openssh.com
  MACs hmac-sha2-512-etm@openssh.com,hmac-sha2-256-etm@openssh.com
