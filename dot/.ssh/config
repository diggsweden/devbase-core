# SSH Configuration Template
# DevBase default SSH configuration with FIPS-140-3 compliance
#
# To add SSH hosts:
#   - Custom/Organization: ~/.config/ssh/custom.config
#   - Personal: ~/.config/ssh/user.config
#
# User config can override custom config settings

# FIPS-140-3 compliance (comment out for non-FIPS mode)
Include ~/.ssh/fips-client.config
# For strongest-only algorithms, use:
# Include ~/.ssh/fips-strong-client.config

Host *
  HashKnownHosts yes
  AddKeysToAgent yes
  StrictHostKeyChecking yes
  VisualHostKey yes

# Custom/Organization SSH hosts (from devbase-custom-config/ssh/)
Include ~/.config/ssh/custom.config

# Personal SSH hosts (can override above)
Include ~/.config/ssh/user.config