# SPDX-FileCopyrightText: 2025 Digg - Agency for Digital Government
#
# SPDX-License-Identifier: CC0-1.0
name: Test Ubuntu Installation
on: # yamllint disable-line rule:truthy
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  test-ubuntu:
    runs-on: ubuntu-26.04
    name: Test on Ubuntu 26.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Cache Mise tools
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.local/share/mise/installs
            ~/.local/share/mise/downloads
          key: mise-ubuntu-26.04-${{ hashFiles('dot/.config/mise/config.toml') }}
          restore-keys: |
            mise-ubuntu-26.04-
      - name: Cache VS Code extensions
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.vscode/extensions
            ~/.config/Code/User
          key: vscode-ext-ubuntu-26.04-${{ hashFiles('dot/.config/devbase/packages.yaml') }}
          restore-keys: |
            vscode-ext-ubuntu-26.04-
      - name: Cache binary downloads
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/devbase/debs
          key: binary-downloads-ubuntu-26.04-${{ hashFiles('dot/.config/devbase/packages.yaml') }}
          restore-keys: |
            binary-downloads-ubuntu-26.04-
      - name: Prepare binary cache directory
        run: mkdir -p ~/.cache/devbase/debs
      - name: Run installation
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GIT_NAME: DevBase CI
          GIT_EMAIL: ci@devbase.test
          DEVBASE_DEB_CACHE: ~/.cache/devbase/debs
        run: |
          export DEBIAN_FRONTEND=noninteractive
          export CI=true
          ./setup.sh --non-interactive
      - name: Verify installation
        run: |
          sudo -v
          ./verify/verify-install-check.sh
